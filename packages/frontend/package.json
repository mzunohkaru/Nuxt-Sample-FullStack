{
  "name": "frontend",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --port 3000",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "eslint .",
    "test": "vitest"
  },
  "dependencies": {
    "@nuxt/ui": "^2.11.1",
    "@pinia/nuxt": "^0.5.1",
    "nuxt": "^3.8.4"
  },
  "devDependencies": {
    "@nuxt/eslint-config": "^0.2.0",
    "eslint": "^8.55.0",
    "vitest": "^1.0.4"
  }
}
